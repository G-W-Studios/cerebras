Selectable filter chip with a mono uppercase label and sharp corners.

```jsx
<Chip selected>All</Chip>
<Chip>Inference</Chip>
<Chip icon="plus">Add filter</Chip>
```

`selected` fills with ink; idle is hairline-outlined.
