Small count or status badge in a mono face.

```jsx
<Badge tone="orange">NEW</Badge>
<Badge tone="dark">3</Badge>
<Badge tone="green">LIVE</Badge>
```

Tones: `orange` / `dark` / `green` / `neutral`.
