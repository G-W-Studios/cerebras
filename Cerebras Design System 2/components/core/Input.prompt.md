Single-line text or search field. Sharp corners, hairline border, orange focus ring.

```jsx
<Input placeholder="you@company.com" />
<Input icon="search" placeholder="Search docs…" />
```

Pass any native input prop (`value`, `onChange`, `type`). `wrapStyle` styles the bordered shell.
