Dark site footer — ink surface, mono uppercase column headers, link columns, newsletter signup and the logo + legal row.

```jsx
<Footer />
<Footer columns={[{ head: "Product", links: ["Inference", "Training"] }]} />
```

White-on-ink. Links hover to orange. Composes `Logo`, `Input`, `Button`.
