Signature Cerebras isometric line-art illustration (black/white/orange). Use these to anchor feature blocks, value props and section intros — never redraw them by hand.

```jsx
<Illustration name="wafer-scale-architecture" size={240} />
<Illustration name="high-throughput" size={180} basePath="../../assets/illustrations" />
```

Names: `custom`, `education`, `process`, `solutions`, `strategy`, `wafer`, `high-throughput`, `wafer-scale-architecture`. Set `basePath` relative to the page (it defaults to a project-root page). They sit best on white or cream.
